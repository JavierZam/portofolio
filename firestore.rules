rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Guestbook: anyone can read, anyone can create (but not update/delete)
    match /guestbook/{messageId} {
      allow read: if true;
      allow create: if request.resource.data.keys().hasAll(['name', 'message', 'createdAt'])
                    && request.resource.data.name is string
                    && request.resource.data.message is string
                    && request.resource.data.name.size() <= 50
                    && request.resource.data.message.size() <= 500;
      allow update, delete: if false;
    }

    // Bug Raid: anyone can read/write the boss document
    match /bug_raid_boss/{docId} {
      allow read: if true;
      allow create, update: if true;
    }

    // Bug Raid: anyone can read, create, and update contributor scores
    match /bug_raid_contributors/{docId} {
      allow read: if true;
      allow create, update: if true;
    }
  }
}
